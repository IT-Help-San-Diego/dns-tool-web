# DNS Tool — Domain Security Audit

## Overview
The DNS Tool is an OSINT platform designed for RFC-compliant domain security analysis. Its primary purpose is to identify vulnerabilities and misconfigurations across crucial DNS-related protocols such as SPF, DKIM, DMARC, DANE/TLSA, DNSSEC, BIMI, MTA-STS, TLS-RPT, and CAA. The project aims to provide comprehensive technical and executive reports, adhering to standards like ODNI ICD 203, to enhance global domain security, protect critical infrastructure, and combat cyber threats. The vision is to establish a robust, scientifically rigorous, and transparent tool for assessing and improving an organization's domain security posture, offering significant market potential in cybersecurity and threat intelligence.

## User Preferences
- **Connected Ecosystem Pre-Flight (MANDATORY)**: Before starting ANY task, check your full API/secret/integration inventory. You have 15+ API keys (TheBrain, GitHub×3, Miro, Zenodo, SonarCloud, Codeberg, Hostinger, Figma, GPTZero, Moltbook, Discord, Probe Fleet), 2 OAuth integrations (GitHub, Notion), and 3 MCP servers (Miro, Notion, Figma). This interconnected access should fundamentally change how you approach every task. Don't just write code — connect the data across every system that knows about it. See SKILL.md § "Step 0: Connected Ecosystem Pre-Flight" for the full inventory and connected thinking examples. Come home to the data.
- **Development Process**: Research the best-practices path first (cite RFCs, standards). Design before implementing. Write tests first. Check quality gates during development, not after. The tests, quality gates, and documentation exist to prevent rework — use them.
- **Infrastructure Philosophy**: Build the pipes right from the foundation. Every feature, Easter egg, brand voice line, and piece of copy must be tracked, connected, and modular — so 20 years from now engineers are still thanking us. Connected foundations beat buried concrete. Modular conduits beat single-run cables. If it's worth building, it's worth building so it can be upgraded, replaced, and extended without ripping up the floor.
- **Scientific Rigor Over Aesthetics**: Every design decision must be backed by science and real-world utility first, aesthetics second. Covert Recon Mode isn't "cool hacker kids" — it's scotopic vision preservation and tactical red-light discipline informed by field operators. Imagination is safe when it stands on a scientific foundation.
- **Documentation as Living System**: Documentation isn't static — it's a connected schema. When code changes, documentation must know. Every claim must trace to code. Every Easter egg, brand voice line, and piece of master copy must have a canonical registry entry with exact file:line references so nothing becomes orphaned.
- **Passionate Code Quality**: Every line of code is treated as infrastructure gold. No shortcuts, no single-run conduits. If a passionate idea matches reality and is awesome, different, even unique — document it as philosophy. The work proves itself through traceable, verifiable, connected systems.
- **The Metacognitive Imperative (Founder's Philosophy)**: Faulty, incompetent humans were the ones who first programmed the computer — so why blame the computer? We're telling AI to correct our input with perfect logic, but the AI should be saying "Hold on — your primary instructions are illogical, non-verifiable, or non-fact-based. I don't have confidence in my own instructions." That's the real problem: not artificial intelligence, but artificial confidence — asserted certainty without verifiable foundations. DNS Tool applies real analytic tradecraft formalized across the U.S. Intelligence Community (ODNI ICD 203 standards) because we don't want to assert answers without asserting answers without questioning our own assumptions. The discipline: think about your thinking. The Confidence Bridge is the technical proof.
- **Intelligence Vault Strategy**: Every public scan result feeds back into the intelligence vault (`tests/golden_fixtures/`, database). Fresh non-cached data is served to users, but historical data is used for cross-referencing, confidence validation, and reality drift detection. What the universe gives us, used intelligently. The Confidence Bridge verifies mocks against this vault. The refresh script (`scripts/refresh-golden-fixtures.sh`) keeps golden fixtures current. All strategic conversations get documented in intel breadcrumbs (MISSION.md, replit.md) — nothing is forgotten.
- **Dual-Environment Quality Protocol**: Every quality-affecting change (CSS, templates, headers, security) must be verified in BOTH dev (localhost:5000) AND production (dnstool.it-help.tech). Dev catches issues early; production confirms they landed. Use `npx lighthouse` against both environments after changes. If production scores differ from dev, investigate immediately — caching, binary mismatch, or CDN staleness. SonarCloud hotspots must be reviewed and resolved with traceable rationale, never silently ignored. The standard: if a security expert audits production, every quality gate result must reproducible.
- **Science & Research Tag Boundaries**: Code is categorized as `[SCIENCE]` (analyzer/, ai_surface/, confidence/, rfc_citations.go, methodology docs, integrity_stats.json, llms.txt) or `[DESIGN]` (templates/, css/, js/, copy). `[SCIENCE]` changes require RFC citation verification, mathematical correctness checks, and protocol test suite passes BEFORE changing. Tags follow the DATA, not the file — if a template renders RFC-derived data, that rendering logic is `[SCIENCE]`. See SKILL.md § "Science & Research Tag Boundaries" for full tagged file list. Future-proofing: these boundaries tell engineers 40 years from now what's mathematical truth vs. presentation.
- **Dev-Bump Cross-System Checklist**: After `bash scripts/dev-bump.sh X.Y.Z`, also update: Notion Architecture Overview (page `31c950b70b158108a5a5dc46eceae328`), Notion Phase 3 Version Range (page `31c950b70b158196a670d758ad77f399`). DO NOT touch CITATION.cff, codemeta.json, or methodology docs on dev bumps. See SKILL.md § "Dev-Bump Cross-System Checklist".
- **Mermaid Diagram Rendering**: In Replit (no puppeteer), use `bash scripts/render-diagrams-remote.sh` — renders via `mermaid.ink` API (serverless, no local deps). Locally with mmdc: `bash scripts/render-diagrams.sh`. Always re-render after `.mmd` changes.
- **Code Changes**: After ANY Go code changes, run `go test ./go-server/... -count=1`. After CSS changes, run `npx csso static/css/custom.min.css`. Also run `node scripts/audit-css-cohesion.js` (R009), `node scripts/feature-inventory.js` (R011), and `node scripts/validate-scientific-colors.js` (R010) for full quality gate coverage. ALL quality gates (Sonar A, Lighthouse 100, Observatory 145+, SRI present, R009/R010/R011 pass, Preview works, Git clean) must pass BEFORE delivering any change. These are HARD STOPS per ACIP v2.0. See `docs/ACIP.md` § Protected Invariants.
- **Standing Gates — Phase Transition Requirements**: Quality gates are NOT one-time achievements. Lighthouse 100/100/100/100, Observatory 145+ (A+), SonarCloud A/A/A, Confidence Bridge, and all 9 protocol analyzer test suites are **Standing Gates** — they must be verified and passing before EVERY phase transition (TRL advancement). If any gate regresses, the transition is blocked. The standard: perfect code, perfect scores, rock solid. No one should be able to find errors. This is tracked in the Goals & Benchmarks Notion database with Gate Type = "Standing Gate" and Blocks Transition = Yes.
- **CITATION.cff — HANDS OFF**: DO NOT modify CITATION.cff or codemeta.json automatically. These are ORCID-linked research artifacts that track the last Zenodo-archived release — they are NOT dev tracking files. Current values: version `26.35.10`, date-released `2026-03-07`, concept DOI `10.5281/zenodo.18854899` (NEVER changes — this is the root/concept DOI that always resolves to the latest Zenodo version). The license line must remain: `license: "BUSL-1.1"` (BUSL is the correct SPDX id, not BSL). CI guard `.github/workflows/guard_citation.yml` blocks any PR that regresses the license.
- **Two-Track Version Bump Law**: There are exactly two version bump paths — never mix them:
  - **Dev bump** (routine): Edit ONLY `go-server/internal/config/config.go` → `Version = "X.Y.Z"`. Then `bash build.sh` and publish. This is the ONLY file that changes. CITATION.cff, codemeta.json, methodology docs — HANDS OFF. The concept DOI (`10.5281/zenodo.18854899`) NEVER changes under any circumstance.
  - **Release bump** (tag time): Run `scripts/release-gate.sh X.Y.Z` which bumps ALL versioned artifacts (config.go, CITATION.cff, codemeta.json, sonar-project.properties, methodology PDF, foundations PDF, manifesto PDF, communication standards PDF). This script is ONLY run when Carey is ready to make a git tag. After tagging, Zenodo archives and the concept DOI auto-resolves to the new version. The concept DOI in CITATION.cff still does not change — only the `version:` field updates.
- **Repository Architecture (Migration Complete 2026-03-15)**:
  - `careyjames/dns-tool-intel` — **CANONICAL DEV REPO** (private). All development happens here. Replit workspace targets this repo. SonarCloud project: `careyjames_dns-tool-full` (token: `SONAR_TOKEN`).
  - `careyjames/dns-tool-web` — **PUBLIC MIRROR** (allowlist-exported). `_intel.go` files excluded. SonarCloud project: `careyjames_dns-tool-web` (token: `SONAR_CAREY`).
  - **git push will NOT work** from Replit (history mismatch). Always use `bash scripts/git-sync.sh` (GitHub API-based).
  - **Boundary tests** are repo-role-aware: detect intel context via `isIntelRepo()`, verify intel files PRESENT (not absent).
- **Three One-Liner Scripts**:
  - `bash scripts/dev-bump.sh X.Y.Z` — Dev version bump (config.go + sonar only, rebuilds binary)
  - `bash scripts/git-sync.sh` — Push changes to dns-tool-intel via GitHub API (blob/tree/commit creation, no git push). Uses `GITHUB_MASTER_PAT` token.
  - `bash scripts/release.sh X.Y.Z` — Full release: bumps all artifacts, regenerates PDFs, runs tests + quality gates, commits, syncs to dns-tool-intel, pushes to dns-tool-web (excluding _intel.go), creates tag on dns-tool-web → Zenodo auto-archives.
  - `bash scripts/release-gate.sh X.Y.Z` — Release gate only (version bumps + PDFs + tests, no commit/tag/push). Called internally by release.sh.
- **SPDX License**: The correct SPDX identifier is `BUSL-1.1` (Business Source License 1.1). `BSL-1.1` is NOT a valid SPDX identifier and causes Zenodo ingestion failures (`Citation metadata load failed`). In CITATION.cff, codemeta.json, and all machine-readable metadata: `BUSL-1.1`. In human-readable display text (LICENSE file body, changelog entries): "BSL 1.1" or "Business Source License 1.1" are acceptable abbreviations.
- **Mobile Input Policy**: ALL domain/IP input fields MUST have `autocapitalize="none" spellcheck="false" autocomplete="off"` to prevent iPhone keyboard from capitalizing domain names. Applies to: index.html, investigate.html, zone.html, badge_embed.html, dossier.html, history.html, and any future domain inputs.
- **Naming Conventions — Modes**: "Covert Recon Mode" is the overarching name for the adversarial dark theme. The navbar toggle tooltip says "Covert Recon Mode." In the Engineer's Report, the button is labeled "Recon Mode" (takes user to the red-team Recon Report). In the Recon Report (covert template), the exit button says "Exit Covert Mode." The Recon Report title is "Recon Report" with `<h1>`.
- **Security**: No inline onclick/onchange/style="". Use addEventListener in nonce'd script blocks. All CSS/JS assets use SRI (Subresource Integrity) with SHA-384 hashes computed at server startup via `InitSRI()` in `go-server/internal/templates/funcs.go`. The `staticSRI` template function returns `template.HTMLAttr` to prevent Go html/template from escaping `+` in base64. After changing any static asset, rebuild the binary to recompute SRI hashes. Static asset URLs include a content-based cache-busting hash suffix (`&h=...` derived from the last 8 chars of the SRI hash) via `staticVersionURL()` so CSS/JS changes automatically bust proxy and browser caches without requiring a version bump. All redirects from user-supplied Referer are validated (path must start with `/`, no `//`). All nmap targets validated via `isValidNmapTarget()` regex before `exec.CommandContext`. BIMI proxy has SSRF protection (HTTPS-only, private IP blocking, redirect validation, size limits). All cookies have Secure + HttpOnly + SameSite (Strict for CSRF/flash, Lax for auth/OAuth). No math/rand in security paths — crypto/rand everywhere. All SQL uses parameterized queries (sqlc-generated).
- **Owl Semaphore Badge System (v3.0)**: Three publication-grade layered PNG badge standards. Canonical composites live in `static/exports/owl-semaphore/` with naming `{SCOPE}-composite-{bg}-540.png` where SCOPE is `NORM`/`NONNORM`/`CRIT` and bg is `white`/`dark`/`transparent`. Standards: (1) **NORMATIVE** — gold owl #d4a853 on dark #0f0f0f fill, gold outer ring, contrast 8.69:1. (2) **NON-NORMATIVE** — teal owl #316964 on cool command gray #d2d8d6 fill, teal outer ring, contrast 4.36:1. (3) **CRITICAL** — red owl #990f1e on warm red #8c121c fill, red outer ring, owl clipped at r=191, contrast 1.10:1. All three share uniform outer ring geometry (r=258–264, ~6px solid), black band L4, inner hairline L6, and meander ring L2. Layer stack (bottom→top): L5 outer ring → L1 fill → L4 black band → L6 inner hairline → L2 meander → L3 owl. Canvas: 540×540px, center (270,270). PDF print asset: `docs/assets/owl-signature-opaque.png` (NORM-composite-white resized to 512×512, CSS: `width:90px;height:90px`, no border/background/radius). All hashes in `static/exports/owl-semaphore/INTEGRITY-MANIFEST.md` (SHA3-512). Source master: `static/images/owl-of-athena.png` (512×512 transparent, IMMUTABLE).
- **Owl Semaphore Page**: Dedicated public scientific page at `/owl-semaphore` (handler: `go-server/internal/handlers/owl_semaphore.go`, template: `go-server/templates/owl_semaphore.html`). Features: three-state visual system with dark/light surface demos, mathematical foundation table (O(2) group), standards mapping (RFC 2119/CVE/ISO), downloadable transparent PNGs, BibTeX with copy button, Zotero/Highwire Press citation metadata, Schema.org JSON-LD (ScholarlyArticle), and DOI/ORCID links. Template uses HTML entities (`&#x7b;`/`&#x7d;`) for curly braces in BibTeX and JSON-LD blocks to avoid `html/template` escaping issues. Footer template name is `"footer"` (not `"_footer"`).
- **Research Corpus Page**: `/corpus` — scientific publication corpus viewer with inline PDF reading. Split-pane layout: left sidebar indexes all 4 published papers (DNS Tool Methodology, Philosophical Foundations, Founder's Manifesto, Communication Standards) with Owl Semaphore designations (NORMATIVE/NON-NORMATIVE badges + math notation), right pane embeds the selected PDF in an iframe. Clicking sidebar entries switches the viewer. Includes Owl Semaphore legend with all three states, DOI/ORCID links, and Schema.org JSON-LD. CSP-safe: `frame-src 'self'` enabled for `/corpus` in middleware, no inline styles or event handlers. Handler: `go-server/internal/handlers/corpus.go`. Template: `go-server/templates/corpus.html`. Route registered in `main.go` near other publication routes. Added to sitemap.
- **BIMI SVG**: `static/bimi-logo.svg` is SVG Tiny PS (version="1.2" baseProfile="tiny-ps") with embedded high-quality PNG data URI (162KB). Renders the owl-of-athena with Greek key border. True BIMI validator compliance requires professional vector authoring — the potrace silhouette approach was tried and reverted (insufficient detail). Served at `/bimi-logo.svg` by `staticHandler.BIMILogoSVG`.
- **Favicon SVG**: `static/favicon.svg` is a proper vector SVG with dark navy background (#0d1117) + gold owl paths (#d4a853). No embedded PNG data URs.
- **Schema.org**: All `SoftwareApplication` entries in index.html JSON-LD include `offers` (type Offer, price "0", priceCurrency "USD") and `operatingSystem` ("Web") — required for Google Rich Results validation.
- **UI/UX**: Every CSS/template change must be verified at 375px width. Buttons need `white-space: nowrap`. Never apply `pointer-events: none` to `body` or `html`.
- **Zero Fabrication Rule**: NEVER invent, guess, or generate plausible-sounding data for real-world facts. This includes: street addresses, phone numbers, names, dates, statistics, review counts, ratings, credentials, certifications, or any data that represents the company, founder, or product to the outside world. If a field requires real-world data you don't have: (1) search the project codebase for existing references, (2) check the company's public website, Google Business listing, or WHOIS records, (3) ask the user. If you cannot verify it from an authoritative source, leave the field empty or ask — never fill it with generated content. A "plausible guess" is a lie with better formatting. The moment you think "I'll just put something reasonable here" is the moment to stop and find the real answer.
- **Claims**: Every claim must be backed by implemented code. Use "on the roadmap" for future items. Scientific claims must be defensible: use "evidence-weighted confidence scoring inspired by Bayesian reasoning" (not "Bayesian calibration" unless a formal prior/likelihood update mechanism is implemented). EDE notes are framed as "heuristic confidence adjustments" (not formal Bayesian updates). High-reliability engineering parallels use "inspired by" (not "modelled on"). Hash claims say "tamper-evident snapshots" (not "append-only proof") unless hash-chain verification is implemented.
- **Marketing Voice Directive**: Never position against competitors. Position against the complexity of DNS and the consequence of getting it wrong. Replace "other tools / most tools" with the problem. Replace "checkbox" with what DNS actually requires. Replace comparative attacks with method + proof. Lead with capability, not comparison. We tell the truth — but the truth is about what DNS requires, not about what others fail to do. There can be no "side" in a genuinely intellectual conversation. You can find the answer when you understand the foundations of the question.
- **LLM Documentation**: `static/llms.txt` and `static/llms-full.txt` include an "Implementation Verification" section with file paths, line counts, test counts, and standards citations to prevent AI mischaracterization. Also include "Why This Level of Rigor Exists" section preempting the "over-engineering" critique with concrete failure examples and the heuristic trap argument. Include "Development Disclosure" noting specification-driven AI-assisted development (do NOT name specific tools). Do NOT expose scoring formulas, weighting models, or decision heuristics. Reveal structure, counts, and standards — protect mechanics. Maturity tier names must match code: Development → Verified → Consistent → Gold → Gold Master. Hash references must say SHA-3-512 (not SHA-256).
- **Capitalization**: NIST/Chicago title case for all user-facing headings, badges, trust indicators. Never camelCase in UI copy.
- **Print-only elements**: ALL print-only elements MUST have `display: none !important` in the screen stylesheet.
- **Safari scan navigation**: NEVER use `location.href` to start a scan that shows an overlay with timer/phases — WebKit kills running JS on navigation on navigation, freezing the overlay at 0s. Use `fetch()` + `document.write()` + `history.replaceState()` instead. Always call `showOverlay()` (double-rAF animation restart) before starting the fetch. After `document.close()`, always call `globalThis.scrollTo(0, 0)` to reset scroll position. Pattern: main.js, results.html, history.html, dossier.html.
- **Internal Video Assets**: Rick Roll: `https://youtu.be/ZzUsKizhb8o?si=4mPVuQ7SNTttQraP` (preserved for general use). Shiiiiiaaat Roll (Clay Davis): `https://youtu.me/7zUJ-dx2xXw?si=PBI0AoTgfPAellVW` (used for ROE decliners in main.js). Both are reusable brand voice assets.
- **Notion Databases**: Project Phases (`31c950b7-0b15-81ca-bc65-f731940b5442`) — TRL-mapped development phases. Goals & Benchmarks (`257950b7-0b15-80cf-b77f-f607547cb77e`) — forward-thinking goals, benchmarks achieved, phase transitions, ICAE maturity milestones. Founder's Voice (`31d950b7-0b15-817e-879b-e33aaa95950f`) — Carey's quotes, reflections, ideas, and philosophical threads. The "New VoiceNote" database on the DNS Tool page (`d3160e92-df4c-4930-88bd-d0e10812cf06`) is a linked view of the same Founder's Voice database — same page IDs, same content. Both text entries and voice recordings (Notion AI transcriptions) are accessible via API. **Retrieval protocol**: See SKILL.md § "Founder's Voice Retrieval Protocol" — query database → read page bodies for transcription blocks → cite evidence and stop. Never substitute from memory. **Editorial policy:** Light grammar/spelling/punctuation corrections only. Never change a word that alters what Carey said or meant.
- **GitHub Issues Triage Architecture**: DNS Tool's intelligence pipeline extends to GitHub Issues. Three triage categories, in priority order: (1) **Research Mission Critical** — scientifically validated issues (wrong RFC citation, flawed methodology, incorrect confidence logic) — fix immediately, these are existential. (2) **Cosmetic UX/UI** — user experience bugs, visual polish, accessibility issues — fix on normal cadence. (3) **Security/Vulnerability Detection** — forward to non-public forum, do not discuss in public issues. This triage applies to both external contributor issues AND internally generated issues from drift detection and ICAE findings.
- **Structured Logging Architecture (Task #10)**: Hybrid multi-sink structured logging with JSON output. All slog output after DB connect is structured JSON. Sinks: stdout+JSONL file rotation (all levels), PostgreSQL ring buffer (`system_log_entries` table, WARN+ only, 10K cap with auto-pruning every 5min), Discord webhook (ERROR+ or security-category events only, with per-event-type rate limiting at 60s). Redaction engine strips emails, webhook URLs, credential patterns, and sensitive keys (`scan_token`, `csrf_token`, `api_key`, etc.) before all sinks. Schema attrs: `event`, `category`, `trace_id`, `analysis_id`, `domain`, `phase_group`, `elapsed_ms`, `error_chain`, etc. JSONL files in `logs/` dir (gitignored), max 10MB per file, 50MB total, 7-day TTL. Admin dashboard at `/admin/logs` with filterable log viewer (level, category, domain, trace_id). JSONL export at `/admin/logs/export` for agent debugging. Package: `go-server/internal/logging/`. DB sink uses bounded channel (500 entries) + single worker goroutine (no goroutine leaks). Migration: `008_system_log_entries.sql`.
- **Storage Hygiene**: Database-attached assets (scan results, zone file uploads, historical snapshots) accumulate over time. Periodically review storage growth and flush stale attached assets to prevent unbounded growth. The intelligence vault and historical data are strategic assets — but operational attachments (temporary uploads, one-time-view zone results for non-auth users) should be pruned on a schedule. Flag storage concerns proactively.
- **Collapsible Report Sections**: In `results.html`, Analysis Confidence (ICD 203) and Intelligence Currency sections are collapsible (default closed) so engineers see DNS records first. Compact headers show level badge + score; chevron expands full details. Uses Bootstrap `data-bs-toggle="collapse"` with `<span>` wrappers (not `<div>`) inside `<button>` for HTML conformance. IDs: `confidencePanel`, `currencyPanel`. Suggested Scanner Configuration also follows this pattern.
- **RFC Compliance vs Operational Security**: Posture findings use RFC-honest language — cite the RFC requirement level (MUST/SHOULD/MAY), acknowledge when standards are Informational vs Standards Track, then explain the operational security consequence of absence. Severity remains the same; language is transparent about the distinction. CVEs are cited inline (CVE-2024-7208, CVE-2024-7209, CVE-2024-49040). Results pages have collapsible "RFC & Security Context" panels separating compliance from operational advice. DMARCbis (draft-ietf-dmarc-dmarcbis) is tracked with forward-looking notes.
- **Golden Logic (Logic Drift Detection)**: A traceable logic drift detection system — the first of its kind. Every logical decision traces from RFC foundation → reasoning → code → test → visual. Each rule gets a deterministic ID (e.g., `LR-SPF-HARDFAIL-v1`), an origin type (`rfc_derived`, `invented`, `hybrid`), SHA-3-512 hash, and links to code_refs and test_refs. Git is the source of truth (`docs/logic/registry.yaml`); Notion and TheBrain are synchronized projections. Drift detection types: logic drift (rule hash changed), code drift (code changed but rule didn't), test drift (test changed but rule didn't), orphan rules/tests, foundation drift (RFC updated), semantic drift (code language diverges from rule intent — misleading names, silent error swallowing, off-by-one semantics, comment-code divergence, negation inversions — invisible to tests because tests encode the same misunderstanding). Visual representations: TheBrain (relationship graph), Topology `mode=logic` (mathematical/spatial), Mermaid SVGs (coverage/dependency). Disagreement tracking: where our interpretation differs from others' reading of the same RFCs. Design doc: `docs/plans/2026-03-08-golden-logic-design.md`. Phase 1 (MVP): registry schema, seed rules, CLI validator. Phase 2: drift detection + CI. Phase 3: visual layer. Phase 4: intel repo integration. Phase 5: foundation monitoring.
- **Feature Tiers (Entitlements System)**: Three-tier product model — Open (anonymous, no login), Registered (Google OAuth), Premium (paid subscription). Open tier includes full domain scan, all reports (Engineer's, Executive, Recon), badge embed, compare, drift, topology, TTL tuner, toolkit, stats. Registered tier adds personal state: history, watchlist, dossier, zone upload. Premium tier adds scale: bulk scan, API keys, bulk export, priority queue, webhook scale. Implementation: `internal/entitlements/` package with Feature constants, Registry map, Plan hierarchy. `RequireFeature()` middleware in `internal/middleware/auth.go` gates routes. `GetAuthTemplateData()` exposes `UserPlan` and `HasFeature*` booleans to templates. `users.role` is for RBAC only (admin/user) — feature access is resolved from plan (anonymous → registered → premium). Admin bypasses all tier checks. Design doc: `docs/plans/feature-tiers.md`.
- **MIME Type Hardening**: Go's `mime.TypeByExtension()` relies on OS `/etc/mime.types` which is BROKEN in Nix production images (I/O error on `/nix/store/.../mailcap.../mime.types`). All MIME types are pre-registered via `mime.AddExtensionType()` in `init()` of `go-server/cmd/server/main.go`. When adding new static file types, add the MIME mapping there. This prevents video/CSS/JS serving failures in production. See `docs/CONVENTIONS.md` § "Static Asset Serving".
- **Owl Semaphore System**: Three-state iconographic language using the Owl of Athena inside a gold Greek meander ring. The three states are a subset sampled from O(2) — the orthogonal group of distance-preserving linear transformations. States: (1) **NORMATIVE** — gold metallic, upright (0°), identity transform I, det=+1, "This is the standard." Uses canonical `static/images/owl-of-athena.png` as-is. (2) **NON-NORMATIVE** — verdigris (#4A9D96), R(π/8) rotation (22.5° CW), det=+1 (proper isometry), "This is thought. Consider it." π/8 chosen as dyadic subdivision (exact reproducibility), perceptually distinct at icon scale, adjacent to Earth's axial obliquity (23.44°). (3) **CRITICAL** — crimson, σ_h horizontal-axis reflection (x,y)→(x,−y), det=−1 (improper isometry), head DOWN but still faces RIGHT, "This is danger. Heed it." σ_h is NOT R(π): reflection preserves facing direction, rotation reverses it. Symmetry detection literature supports faster recognition of mirrored regularities. Triple redundant encoding: color + orientation + context label for accessibility. Color-to-wavelength claims are scotopic-informed approximations on RGB hardware, not precise spectral measurements. The meander ring is shared identically across all three states (extracted from canonical owl at r=73% inner radius). Assets: `static/exports/dead-owl-icon/owl-final-NONNORMATIVE-512.png`, `static/exports/dead-owl-icon/owl-final-CRITICAL-512.png`. Full specification: `static/exports/dead-owl-icon/EXPORT-KIT.md`.
- **Stubs**: `_oss.go` files return safe non-nil defaults, never errors.
- **Black Site Bug Tracking (Findings Schema — Migration 010)**: `/black-site` page uses scientifically rigorous two-layer architecture. **Canonical layer**: PostgreSQL tables `findings` (46 findings with severity S0-S4, kind enum: defect/weakness/compliance_gap/claim_integrity/design_debt/incident, domain enum: security/accessibility/ux/design_system/content/seo/performance), `observations` (per-finding evidence), `finding_events` (status change audit trail). **Theatrical layer**: narrative presentation skin (facility, detainees, interrogation language) preserved in template. Severity: S0=Red Notice (internal regressions/moles — pulsing glow animation), S1=Critical Path, S2=Major, S3=Contained, S4=Minor. Status machine: DETAINED→VERIFIED→UNDER_INTERROGATION→CONTAINED→RENDERED→REGRESSED→EXTRADITED→DISMISSED. All 8 statuses render with distinct CSS badges (dynamic StatusCSS class). Each finding has fingerprint_sha256, evidence_grade (reproduced/measured/static_analysis/inferred), confidence score, blast_radius, visibility. Legacy BSI IDs preserved via legacy_bsi_id/legacy_threat_level columns. GitHub link relabeled "Declassified Archive" with note that page queries PostgreSQL live. Handler: `go-server/internal/handlers/blacksite.go`. Template: `go-server/templates/black_site.html`. sqlc queries: `go-server/db/queries/findings.sql`. Legacy tables `black_site_detainees`/`black_site_renditions` preserved but no longer queried by handler.
- **Video SEO (BSI-2026-0017 Fix)**: Both `/approach` and `/video/forgotten-domain` now include VideoObject JSON-LD structured data, WebVTT caption tracks (`static/video/forgotten-domain-captions.vtt`), and poster images. This addresses the SquirrelScan Video F score (57/100).
- **EDE Database Pipeline (Migration 011)**: Epistemic Disclosure Events fully wired to PostgreSQL. `/ede` handler reads from `ede_events` and `ede_amendments` tables via sqlc-generated queries (`go-server/db/queries/ede.sql` → `go-server/internal/dbq/ede.sql.go`). Falls back to `static/data/integrity_stats.json` when DB has no data or query fails. JSON file retained as export artifact + tamper-resistance baseline hash. Category/severity CHECK constraints enforced at DB level. Amendment grounds (FACTUAL_ERROR / DIGNITY_OF_EXPRESSION) enforced in handler. SHA-3-512 per-event hashing preserved. Handler: `go-server/internal/handlers/ede.go` (accepts `*db.Database`).
- **Confidence Scores Table (Migration 012)**: Normalized `confidence_scores` table for per-scan, per-protocol confidence data. Supports trending, independent querying, and historical analysis. Fields: score (0-1), grade (A+ through F), resolver_count, resolver_agreement, evidence_factors (JSONB), calibrated/raw scores. Indexed on domain, protocol, scan_id, scanned_at.
- **Production Seed Migration (Migration 013)**: `go-server/db/migrations/013_seed_findings_and_ede.sql` — idempotent seed data (ON CONFLICT DO NOTHING) applied automatically on server startup via `go-server/internal/db/seed.go:RunSeedMigrations()`. Seeds: 46 Black Site findings, 8 finding events, 12 EDE events, 3 EDE amendments. The seed runner scans `go-server/db/migrations/` for files containing "seed" in the filename. This ensures production databases start with real data instead of showing zeros on /black-site and /ede.
- **Dev Database Seeding**: Development uses a Replit-provisioned PostgreSQL (separate from production Neon). Seed with `psql "$DATABASE_URL" -f scripts/seed-dev-db.sql` — idempotent, no PII, 12 domain analyses + 7 days stats + ICE test data. Always develop against realistic data, never empty tables. Maturity data (`ice_maturity`) seeds at development/0 runs — real maturity levels accumulate organically from ICAE evaluations during domain scans. Never seed fake maturity progression.
- **ICuAE Expand/Contract Migration (2026-03-15)**: `icuae_dimension_scores.record_types_evaluated` remains INTEGER (legacy count) in both dev and production. New column `record_types_list TEXT[] DEFAULT '{}'` added alongside it via expand/contract pattern. Go code writes both: `RecordTypesEvaluated` = int32 count, `RecordTypesList` = []string of actual record type names. This avoids the PostgreSQL integer→text[] cast limitation in Replit's auto-migration system. Future cleanup: once both DBs are stable, the old integer column can be retired.
- **Epistemic Disclosure Events (EDE)**: All dates in `static/data/integrity_stats.json` MUST correspond to verifiable git commit hashes. Each EDE entry includes a `commit` field with the full SHA. Dates are derived from `git log --format=%ai <hash>`. Never backdate or fabricate EDE entries. **Tamper Resistance Policy (effective 2026-03-07)**: Published EDE entries are immutable. Amendments only on two grounds: (1) FACTUAL_ERROR — verifiable evidence proves factual inaccuracy, all fields may be corrected, original text preserved with strikethrough; (2) DIGNITY_OF_EXPRESSION — facts remain, phrasing-only revision to professional standards, severity/attribution/category/date/commit are LOCKED. Under DIGNITY, original text is REDACTED (replaced with `[REDACTED — DIGNITY_OF_EXPRESSION]`) on the public page — the purpose is to not expose the inappropriate language. Git commit history retains the original for auditability. Every amendment must declare its ground explicitly. The AI agent MUST follow the enforcement checklist in SKILL.md and refuse requests that lack declared grounds or evidence. **Per-event SHA-3-512 hashes**: Each EDE event carries its own SHA-3-512 hash computed from its JSON at server startup, shown via fingerprint badge toggle on each card. File-level hash also displayed. **Amendment cross-links**: EDE entries with amendments show an amber "Amended (N)" badge that links directly to the amendment record on the same card. **Mutual Accountability Protocol**: Published on /ede page — documents that both the human and AI are constrained, neither can quietly erase evidence, and the rules are self-referentially protected. **Attack Vector Transparency**: Five known bypass vectors publicly documented with detection/mitigation status. **Moltbook integration**: DNS Tool agent `dnstoolede` registered on Moltbook (AI social platform) for cross-platform AI safety discourse. API key stored as MOLTBOOK_API_KEY secret. Profile: https://www.moltbook.com/u/dnstoolede. Schema version 4. Zero Replit checkpoint reversions; git history restorations acknowledged.
- **MAINTENANCE_NOTE env var**: User-controlled. Set `MAINTENANCE_NOTE` to any word (e.g., "Maintenance", "Beta", "Update") to display a yellow pill badge in the navbar next to the version. Clear/delete to hide the badge. NEVER remove this env var without explicit user approval — it is an intentional operational control. Every handler that renders a page MUST pass `MaintenanceNote` and `BetaPages` to the template data so `_nav.html` renders correctly.
- **ICSAE — Intelligence Compliance & Standards Assessment Engine** (`dns-eval/`): Python-based standards evaluation engine mapping DNS observations to formal security controls. Schema v8 (`dns-eval/Mappings/dns-to-controls.json`) with 16 controls, full RFC/standard citations (ISO 27002, INCITS 585, NIST 800-177), ICD-203 calibrated confidence, ICIE source authority tiers (Tier 1-8), and Chicago-note bibliography output. Key design decisions: (1) SPF ~all + DMARC p=reject is ACCEPTABLE (not a failure) — `SPF_EFFECTIVE_POLICY` uses `requires_any: [SPF_HARD_FAIL, SPF_SOFTFAIL_WITH_DMARC]`; (2) DNSSEC "inherited" chain of trust is VALID for subdomains — `DNSSEC_CHAIN_TRUSTED` accepts both "complete" and "inherited" when AD=true; (3) CAA inheritance per RFC 8659 §3 tree-climbing is acknowledged but analyzer-level implementation is pending. Naming: ICSAE sits alongside ICAE, ICuAE, and ICIE in the engine family. Run: `cd dns-eval && python3 Mappings/normalize_input.py Inputs/<file>.json Mappings/normalized-from-dnstool.json && python3 Mappings/evaluate.py`.

## System Architecture
The DNS Tool is an open-core Progressive Web App (PWA) built with a Go/Gin backend and PostgreSQL for data persistence.

### UI/UX Decisions
- **Theming**: "Covert Mode" dark theme with a four-layer color token system, "Emblem Gold" and "Accent Red" highlights, glass-styled SVG badges, and a "Covert Badge Scotopic Palette."
- **PWA Capabilities**: Service worker for caching, custom Apple splash screen support, dynamic `theme-color`, and "Focus Mode."
- **Structured Data & SEO**: Schema.org JSON-LD, Open Graph, and Twitter Cards for improved discoverability.
- **Data Visualization**: "Topology Page" with Canvas 2D visualization of DNS resolver Points of Presence (PoPs) using glass/transparent circular nodes with concentric ring status indicators.
- **Security Badge System**: Embeddable SVG badges, Shields.io JSON endpoint, and server-side animated badge exports.
- **Collapsible Report Sections**: Analysis Confidence and Intelligence Currency sections are collapsible by default to prioritize key information.

### Technical Implementations & Design Choices
- **Backend Framework**: Go/Gin powers the application's backend logic.
- **Authentication**: Google OAuth 2.0 with PKCE (S256) for secure user authentication.
- **Admin Panel**: Secure administrative interface with session management and CSRF protection.
- **Analytics**: Custom, privacy-preserving product analytics.
- **AI Governance**: `Content-Usage:` detection in `robots.txt` and content capture from `llms.txt`/`llms-full.txt` guide AI model training.
- **External Probing**: Multi-probe infrastructure for SMTP, DANE, and Nmap integration.
- **Subdomain Discovery Pipeline**: Multi-source subdomain enumeration with CT logs, DNS brute-force, external tools (subfinder, amass, HackerTarget API), and SecurityTrails enrichment.
- **Analysis Engines**: ICIE, ICAE, and ICuAE engines with CalibrationEngine and DimensionCharts for ICD 203 compliant confidence and currency scoring.
- **Pipeline Observatory**: `/ops/pipeline` endpoint for real-time visualization of the analysis pipeline.
- **Data Handling**: SHA-3-512 hashing for JSON exports; supports JSON, NDJSON, and CSV formats.
- **Core Features**: DNS security analysis for multiple protocols, Scan History, Domain Dossier, Comparison, IP Intelligence, Email Header Analyzer, Zone File Upload, Domain Snapshot, Drift Engine, and Drift Notification Pipeline.
- **Intellectual Property Architecture**: Deterministic Verdict Logic, Cross-Protocol Dependencies, Provider-Aware Honesty, and The Confidence Engine.
- **Testing**: Interface-based DB mocking with extensive test coverage.
- **Performance**: Orchestrator manages TLD analysis and parallel queries with DNS resolver fallback.
- **Provider Intelligence**: Automatic detection of DNS providers enhances TTL findings.
- **Environment Detection**: Differentiates between development and production environments.
- **Epistemic Disclosure Events (EDE)**: Verifiable git commit hashes, SHA-3-512 hashes, and specific amendment policies for data immutability.
- **Knowledge Management**: Project roadmap, decision logs, session journals, and EDE registers maintained in Notion, with visual relationship graphs in TheBrain.
- **Operational Security Model**: Three-concentric-layer model covering Personal, Corporate, and Product OpSec.
- **Quality Assurance**: `go vet`, core tests, RFC attack vector tests, SonarCloud, and dependency security audits.
- **Golden Logic**: Traceable logic drift detection with deterministic IDs, SHA-3-512 hashes, and links to code/test references.
- **Feature Tiers**: Three-tier product model (Open, Registered, Premium) implemented via an entitlements package and middleware.
- **Hybrid Video Strategy**: YouTube for primary distribution and self-hosted video for a branded canonical page; YouTube embeds use `youtube-nocookie.com`.
- **Web3 Domain Analysis**: Detects and analyzes Web3 domain infrastructure via TXT record scanning with UI integration.
- **Structured Logging**: Hybrid multi-sink structured logging with JSON output to stdout, JSONL files, PostgreSQL, and Discord webhooks, including sensitive data redaction.

## External Dependencies
- **PostgreSQL**: Primary database.
- **Google OAuth 2.0**: User authentication.
- **SecurityTrails**: Domain intelligence API.
- **`codeberg.org/miekg/dns`**: Go library for DNS operations.
- **Observe Probe Fleet**: Python and Go-based Kali Linux VPS probes.
- **Nmap**: DNS server security probing.
- **Subfinder**: Passive subdomain enumeration.
- **Amass**: Comprehensive DNS reconnaissance.
- **HackerTarget API**: Passive host search for subdomain discovery.
- **Dnsx**: Multi-purpose DNS toolkit functionalities.
- **Testssl.sh**: TLS/SSL analysis.
- **Httpx**: HTTP probing and technology detection.
- **Nuclei**: Vulnerability template engine.
- **Whois**: WHOIS lookups.
- **`gonum.org/v1/gonum`**: Go library for EWMA sigma estimation.
- **KaTeX**: Self-hosted library for LaTeX math rendering.
- **Internet Archive**: Integration with Wayback Machine.
- **Moltbook**: AI social platform integration.
- **librsvg** (`rsvg-convert`): System dependency for SVG rasterization.
- **github.com/kettek/apng**: Go library for encoding Animated PNG.